const routes = {
  auth: {
    signIn: { path: "/sign-in" },
    signUp: { path: "/sign-up" },
    verify: { path: "/verify" }
  },
  user: {
    dashboard: {
      path: "/dashboard",
    },
  },
};

export default routes;
